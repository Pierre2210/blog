<h1>Listing animals</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Description</th>
    <th>Image</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @animals.each do |animal| %>
  <tr>
    <td><%= animal.id %></td>
    <td><%= animal.name %></td>
    <td><%= animal.description %></td>
    <td><%= link_to 'Show', animal %></td>
    <td><%= link_to 'Edit', edit_animal_path(animal) %></td>
    <td><img alt="<%= animal.name %>" src="<%= animal.image %>" title="<%= animal.description %>"/></td>
    <td><%= link_to 'Destroy', animal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <!--<td><%= link_to "TestImage", /home/magency/Workspace/TestRuby/blog/ImageBaseTest/galah.jpg %></td>-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Animal', new_animal_path %>
